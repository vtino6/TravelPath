# Development Profile Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/travelpath_db_dev
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Use H2 in-memory database for quick testing (optional)
# Uncomment below and comment PostgreSQL config above to use H2
# spring.datasource.url=jdbc:h2:mem:travelpath
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
